#version 450

struct SSBO {
    vec3 coeff;
    vec3 power;
    int radius;
};

layout(std430, binding = 0) readonly buffer storageBufferInfoIn {
   SSBO ssbo_in[];
};

layout(std430, binding = 1) writeonly buffer storageBufferInfoOut {
   SSBO ssbo_out[];
};

layout (local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

void main() 
{
    ssbo_out[0].radius = 50;
}